<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SpotifyQueue" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/spotify_green</item>
        <item name="colorPrimaryVariant">@color/green</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/spotify_green</item>
        <item name="colorSecondaryVariant">@color/green</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/circular_std_book</item>

        <item name="materialButtonStyle">@style/CircularButtonStyle</item>
        <item name="actionBarStyle">@style/CircularActionBarStyle</item>
        <item name="snackbarStyle">@style/MySnackbar</item>
        <item name="snackbarTextViewStyle">@style/MySnackbar.Text</item>
    </style>
    <style name="Theme.SpotifyQueue.NoActionBar" parent="Theme.SpotifyQueue">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.SpotifyQueue.DarkerShadow" parent="Theme.SpotifyQueue">
        <item name="android:spotShadowAlpha">0.6</item>
    </style>
</resources>